// TODO implementer Auth page

import { Outlet } from "react-router-dom"

function Auth() {
    return (
        <div>
            Auth page
            <Outlet/>
        </div>
    )
}

export default Auth